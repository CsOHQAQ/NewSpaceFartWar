URP后处理拓展包，基于2021.3.4f1c1版本构建，其他版本可能需要一些更改。

使用例子之前，需要把渲染管线换成包里的。

继承CustomVolumeComponent类来实现自己的后处理特效，参考例子中的写法即可。

这个版本可以的2D渲染器直接支持RenderFeature，如果你的Unity版本不支持，可以尝试以下操作：
1.新建一个Forward渲染器，再新建一个摄像机调成Overlay模式挂在主相机的栈里；
2.主相机不开后处理，新相机开后处理；
3.主相机用2D渲染器，新相机用Forward渲染器；